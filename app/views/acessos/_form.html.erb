<%= form_for(acesso) do |f| %>
  <% if acesso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(acesso.errors.count, "error") %> prohibited this acesso from being saved:</h2>

      <ul>
      <% acesso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ac_id %>
    <%= f.number_field :ac_id %>
  </div>

  <div class="field">
    <%= f.label :ac_user %>
    <%= f.number_field :ac_user %>
  </div>

  <div class="field">
    <%= f.label :ac_tela %>
    <%= f.number_field :ac_tela %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
